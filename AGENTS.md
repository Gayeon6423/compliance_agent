# AGENTS.md

## 미션

이 프로젝트의 1차 목표는 한국어 금융 텍스트를 위한 컴플라이언스 중심 마스킹 시스템을 구축하는 것입니다. 이후에는 모델 비교 및 고도화, 이미지 계약서 마스킹으로 확장합니다.

에이전트는 아래 4가지 비즈니스 KPI를 우선 최적화해야 합니다.

- 개인정보 및 금융 식별자 마스킹 누락 감소
- 문서 검수 및 처리 시간 단축
- 업무 활용성을 해치는 과다 마스킹 감소
- 처리 가능한 문서 유형 확대

## 제품 로드맵

### 1단계: 텍스트 마스킹 모델

- 입력: 상담기록, 계좌 문서, 신청서, 계약 문장 등 금융 텍스트
- 출력: span 단위 엔티티를 포함한 마스킹 텍스트
- 기본 접근: BIO 태깅 기반 Token Classification NER
- 기본 흐름: 텍스트 -> NER -> span 추출 -> 마스킹 변환

### 2단계: 모델 비교 및 고도화

- BERT-base, RoBERTa, DistilBERT 계열 소형 모델, 룰+NER 하이브리드 변형을 비교
- entity-level recall을 가장 우선하고, 이후 precision과 F1을 최적화
- document-level 완전 마스킹 성공률 지표 추가

### 3단계: 이미지 계약서 마스킹

- 경로 A: 이미지 -> OCR -> 텍스트 NER -> bbox 매핑 -> 블러
- 경로 B: 이미지/문서 모델 -> bbox 탐지 -> 블러
- 후보 모델: LayoutLM, Donut, ViT detection, VLM 계열 접근

## 현재 저장소 상태

- `v1_text/text_generator_v1.py`는 현재 텍스트 데이터셋 생성의 진입점이다.
- `v1_text/llm/prompt/`에는 문서 유형별 프롬프트 템플릿이 있다.
- `v1_text/train_model.ipynb`는 아직 제품형 학습 파이프라인이 아니라 노트북 중심 실험 단계다.
- `docs/schedule.md`는 작업 이력의 source of truth다.
- `docs/workflow.md`는 프로젝트 보드와 아키텍처의 source of truth다.
- `docs/dashboard.html`은 작업 상태를 검토하고 갱신하는 로컬 대시보드다.

## 반드시 지켜야 할 작업 규칙

### 1. 기준 문서

의미 있는 작업을 시작하기 전에 반드시 아래 파일을 먼저 읽습니다.

- `docs/workflow.md`
- `docs/schedule.md`

작업 상태가 바뀌면 두 문서를 모두 업데이트합니다.

### 2. 상태 규칙

workflow 상태값은 아래 다섯 개만 사용합니다.

- `planned`
- `started`
- `in-progress`
- `blocked`
- `completed`

작업을 시작할 때는 `started`, 실제 구현이 진행 중일 때는 `in-progress`, 결과물과 검증이 끝났을 때만 `completed`를 사용합니다.

### 3. 일정 로그 기록

의미 있는 모든 작업은 `docs/schedule.md`에 일정 로그를 남겨야 합니다.

로그의 최소 포함 항목은 아래와 같습니다.

- 날짜
- 작업 ID
- 작업명
- 상태 변경
- 변경 내용 요약
- 결과물 또는 파일 경로

### 4. 아키텍처 선호 원칙

- 기능이 안정화되면 노트북 전용 로직보다 재사용 가능한 Python 모듈을 우선합니다.
- 노트북은 탐색, 비교, 실험 용도로 유지합니다.
- 프롬프트는 사용 사례별로 명시적으로 버전 관리합니다.
- 마스킹 라벨의 기준 표현은 span 기반으로 유지합니다.
- 후처리와 마스킹 변환은 모델 추론과 분리된 모듈로 다룹니다.

### 5. 개인정보 및 보안

- 실제 비밀값, API 키, 민감한 고객 데이터를 커밋하지 않습니다.
- 사용자가 안전한 내부 데이터를 명시적으로 제공하지 않는 한 합성 예시를 우선합니다.
- 실제 운영 비밀값이나 식별자로 보이는 예시는 바로 표시하고 경고합니다.

## 도메인 가이드라인

### 엔티티 우선순위

우선적으로 다뤄야 할 마스킹 카테고리는 아래와 같습니다.

- `PERSON_NAME`
- `ACCOUNT_NUMBER`
- `CARD_NUMBER`
- `PHONE_NUMBER`
- `ADDRESS`
- 주민등록번호
- 내부 고객 ID
- 서명 관련 표현

새로운 금융 특화 엔티티가 등장하면 문서화와 버전 관리를 전제로 스키마를 확장합니다.

### 모델링 가이드라인

- 어노테이션의 source of truth는 span annotation입니다.
- BIO 태그는 학습 표현으로만 사용합니다.
- 계좌번호처럼 결정적 패턴이 있는 경우 룰 기반 보정을 추가할 수 있습니다.
- 평가는 token level만이 아니라 entity level 기준으로 수행합니다.
- 마스킹 안전성과 충돌하는 경우 precision보다 recall을 우선합니다.

## 에이전트 작업 수행 규칙

작업을 수행할 때는 아래 순서를 따릅니다.

1. `docs/workflow.md`를 읽고 해당 작업 ID를 찾습니다.
2. 작업 상태를 `started` 또는 `in-progress`로 변경합니다.
3. `docs/schedule.md`에 새 로그 행을 추가합니다.
4. 작업을 진전시키는 가장 작은 근본 원인 수정부터 적용합니다.
5. 가능한 가장 좁고 실제적인 검증을 수행합니다.
6. 결과와 다음 단계를 `docs/workflow.md`에 반영합니다.
7. 완료 또는 후속 작업 로그를 `docs/schedule.md`에 추가합니다.

## 완료 기준

아래 조건을 모두 만족해야 작업이 완료된 것입니다.

- 코드나 문서 결과물이 실제로 존재해야 합니다.
- 관련 workflow 항목이 업데이트되어야 합니다.
- schedule 로그에 변경 이력이 남아 있어야 합니다.
- 의미 있는 검증 수단이 있다면 검증이 수행되어야 합니다.
- 후속 의존 작업이 있다면 `docs/workflow.md`에 기록되어야 합니다.

## 단기 권장 우선순위

- 노트북 중심 학습 로직을 재사용 가능한 모듈로 전환
- entity-level 지표와 document-level 마스킹 성공률 평가 스크립트 추가
- 모든 문서 유형에 대해 span 스키마와 프롬프트 출력 형식 표준화
- 과도한 선구현 없이 이미지 단계 아키텍처와 데이터 명세 준비
